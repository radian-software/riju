id: "zoem"
aliases:
  - "azm"
name: "Zoem"

install:
  apt:
    - zoem

repl: |
  zoem
input: |
  \let{123 * 234}
  .

main: "main.azm"
template: |
  \inform{Hello, world!}

run: |
  zoem -I main.azm; zoem
