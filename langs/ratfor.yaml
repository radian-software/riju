id: "ratfor"
aliases:
  - "rationalfortran"
name: "Ratfor"

install:
  apt:
    - ratfor
    - flang

main: "main.r"
template: |
  print *, 'Hello, world!'
  end

compile: |
  ratfor main.r -o main.f && flang main.f -o main
run: |
  ./main
