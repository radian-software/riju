id: "pug"
name: "Pug"
monacoLang: pug

install:
  riju:
    - prettier
  npm:
    - pug-cli

main: "main.pug"
template: |
  html
    body
      p Hello, world!

compile: |
  pug main.pug
run: |
  prettier --no-config main.html
